# These are all loaded into the environment (os.environ), stored as strings
[INPUT]
# ./DEV/ or ./ANALYST/
WEBPAGES_DIR='./DEV/'

[OUTPUT]
# where to store the partial indexes that are eventually merged
PARTIAL_INDEX_DIR='./partial_indexes/'

[TEST]
# whether or not to run tests before launching
TESTING='true'